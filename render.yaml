services:
  - type: web
    name: node-api
    runtime: node
    plan: free
    buildCommand: yarn install && yarn build
    startCommand: yarn start
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: CORS_ORIGINS
        value: "*"
