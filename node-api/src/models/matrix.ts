/** Una matriz es un arreglo de arreglos de números. */
export type Matrix = number[][];

/** Un conjunto de matrices sobre el que se calculan las estadísticas (p.ej. Q y R). */
export type MatrixSet = Matrix[];
